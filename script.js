// What is Master ?
// By default create branch its called master-branch.

// CLI- Command Line Interface
// GUI- Graphical User Interface

// How to install repository
//     1 -> git add .
//     2 -> git commit -m "initial commit"
//     3 -> git push
//     4 -> git reset head~1 (Use for go one step back)

// git status (check file for in changes area or stages area)
// git log --all --oneline (check for all commits from user)

// git checkout (for go to previous version code)
// git checkout master (for come back to current version code)

// git branch about (for crate a new branch)
// git branch --delete about (for delete any branch)

// git merge contact (contact branch merge in master branch)


// (Link to Github repository using this commands) <-----------------------------------------

// git remote show origin
// git branch -M main
// git push -u origin main
